<div class="container-fluid new-background" style="background-image: url(<%= asset_path('taipei.jpeg')%>)">
 <div class="row">
  <div class="col-xs-12 col-md-6 col-md-offset-3">
    <h1>New booking</h1>
    <%= render 'form' %>
    <p>Price per day: $<span id='day-price'><%= @office.price %></span></p>
    <h4>Total price: $<span id='total-price'>0</span></h4>
    <%= link_to 'Cancel', office_path(@office) %>
  </ul>
</div>
</div>
</div>
