<h1>Your bookings</h1>
<ul>
  <% @bookings.each do |booking| %>
    <li><%= booking.office.name %>. From: <%= booking.start_date %>, to: <%= booking.end_date %> <%= link_to 'edit', edit_booking_path(booking) %></li>
  <% end %>
</ul>
