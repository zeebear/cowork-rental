<%= booking_approve_path(@booking) %>

<%= simple_form_for booking_approve_path(@booking), method: :post do |f| %>
  <%= f.input :status, collection: ['aprove', 'decline'], input_html: { name: 'booking[approve]' } %>
  <%= f.submit :submit %>
<% end %>
